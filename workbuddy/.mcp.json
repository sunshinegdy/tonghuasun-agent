{
  "mcpServers": {
    "tonghuasun": {
      "type": "stdio",
      "command": "node",
      "args": ["${CODEBUDDY_PLUGIN_ROOT}/scripts/tonghuasun-mcp-proxy.mjs"],
      "cwd": "${CODEBUDDY_PLUGIN_ROOT}",
      "env": {
        "TONGHUASUN_MCP_TEXT_COMPATIBILITY": "structured-json"
      }
    }
  }
}
